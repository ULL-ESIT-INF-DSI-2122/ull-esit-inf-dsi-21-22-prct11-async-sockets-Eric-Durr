import { expect } from 'chai';
import { describe, it } from 'mocha';
// import Server from '../src/Server.class';
// import Client from '../src/Client.class';

describe('Client class', () => {
  it('Testing data receiving', () => {
    expect(true).to.be.true;
  });
});
